{
    "title": "Social Media Handles and Contact Info Extractor",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "startUrls": {
            "title": "Base URLs",
            "type": "array",
            "description": "Entry-point URLs for the crawler.",
            "prefill": [{ "url": "http://www.apify.com" }],
            "editor": "requestListSources"
        },
        "proxyConfig": {
            "title": "Proxy configuration",
            "type": "object",
            "description": "Optionally use Apify Proxy.",
            "default": {},
            "editor": "proxy"
        },
        "maxDepth": {
            "title": "Maximum link depth",
            "type": "integer",
            "description": "Upper bound on link depth for crawler to traverse.",
            "prefill": 2
        },
        "maxRequests": {
            "title": "Maximum requests",
            "type": "integer",
            "description": "Upper bound on number of pages the crawler will visit per run.",
            "prefill": 100
        },
        "waitForAjax": {
            "title": "Wait for asynchronous elements",
            "type": "boolean",
            "description": "The crawler will wait until all page elements are loaded.",
            "default": true
        },
        "liveView": {
            "title": "Enable LiveView",
            "type": "boolean",
            "description": "Screenshots of pages will be stored when run in cloud.",
            "default": false
        },
        "considerChildFrames": {
            "title": "Crawl Frames",
            "type": "boolean",
            "description": "The crawler will inspect code loaded inside of HTML frames.",
            "default": true
        },
        "collectSublinks": {
            "title": "Store Sublinks",
            "type": "boolean",
            "description": "The crawler will store any sublinks it finds.",
            "default": false
        },
        "sameDomain": {
            "title": "Stay within Domain",
            "type": "boolean",
            "description": "The crawler will only crawl links within the given domain.",
            "default": true
        }
    },
    "required": ["startUrls"]
}
